All commands :
    set_name: sets username
    names: returns list of all users
    close: closes connection
    help: lists all commands

All working commands:
    set_name: sets username
    names: returns list of all users
        This only works if there is one name in the database, so far
    


